using UnityEngine;

namespace Game.Management.Audio
{
    /// <summary>
    /// Manages audio playback and settings in the game.
    /// </summary>
    /// <remarks>
    /// This class is responsible for handling all audio-related functionalities,
    /// including background music, sound effects, and volume control.
    /// </remarks>
    public class AudioManager : MonoBehaviour
    {


        
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
